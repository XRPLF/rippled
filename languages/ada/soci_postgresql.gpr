project SOCI_PostgreSQL is
   for Externally_Built use "true";
   for Source_Dirs use ();
   for Library_Dir use "../../backends/postgresql";
   for Library_Name use "soci_postgresql";
   for Library_Kind use "static";
end SOCI_PostgreSQL;

