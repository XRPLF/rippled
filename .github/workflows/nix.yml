name: nix
on: [push, pull_request]

jobs:

  test:
    strategy:
      matrix:
        compiler:
          - name: gcc
            version: 11
            cc: /usr/bin/gcc
            cxx: /usr/bin/g++
          - name: clang
            version: 14
            cc: /usr/bin/clang
            cxx: /usr/bin/clang++
        configuration:
          - Debug
          - Release
        cmake-args:
          -
          - "-Dunity=ON"
    runs-on: [self-hosted, linux, rippled-runner-dind]
    container: thejohnfreeman/rippled-build-ubuntu:12e19cd9034b
    env:
      build_dir: .build
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: check environment
        run: |
          echo ${PATH} | tr ':' '\n'
          conan --version
          cmake --version
          env
      - name: configure Conan
        run: |
          conan profile new default --detect
          conan profile update settings.compiler.cppstd=20 default
          conan profile update settings.compiler=${{ matrix.compiler.name }} default
          conan profile update settings.compiler.version=${{ matrix.compiler.version }} default
          conan profile update settings.compiler.libcxx=libstdc++11 default
          conan profile update env.CC=${{ matrix.compiler.cc }} default
          conan profile update env.CXX=${{ matrix.compiler.cxx }} default
      - name: build
        uses: ./.github/actions/build
        with:
          generator: Ninja
          configuration: ${{ matrix.configuration }}
          cmake-args: ${{ matrix.cmake-args }}
      - name: test
        run: |
          ${build_dir}/rippled --unittest --unittest-jobs $(nproc)
