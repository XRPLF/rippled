name: Run pre-commit hooks

on:
  merge_group:
    types:
      - checks_requested
  pull_request:
  push:
    branches:
      - "develop"
      - "release*"
  workflow_dispatch:

jobs:
  # Call the workflow in the XRPLF/actions repo that runs the pre-commit hooks.
  run-hooks:
    uses: XRPLF/actions/.github/workflows/pre-commit.yml@70145243b905dc3e040a61d39c00e178cfb96f71
    with:
      runs_on: ubuntu-latest
      container: '{ "image": "ghcr.io/xrplf/xrpld/pre-commit:sha-f56b79f" }'
