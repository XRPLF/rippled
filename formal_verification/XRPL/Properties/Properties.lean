/-
Import every new proof file here, otherwise `lake build` will not check it. Proofs matter
only at build time, so nothing from this tree is linked into xrpld.
-/
import XRPL.Properties.Protocol.Number.Compare.Compare
